# Deploy preprod — staging.onscen.com apres CI verte sur main/master
#
# Secrets GitHub requis (Settings → Secrets and variables → Actions) :
#   STAGING_SSH_PRIVATE_KEY — cle privee SSH (ed25519) autorisee sur 51.159.170.181
#   (injectee via webfactory/ssh-agent — pas de fichier cle sur le runner)
#
# Declenchement :
#   - automatique quand le workflow "CI" reussit sur main/master
#   - manuel via Actions → Deploy Preprod → Run workflow
#
# Doc : docs/ENVIRONNEMENTS.md

name: Deploy Preprod

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [master, main]
  workflow_dispatch:

concurrency:
  group: deploy-preprod
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy to staging
    runs-on: windows-latest
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push')

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: |
            commun/backend/package-lock.json
            web/app/package-lock.json
            ios/apptel/package-lock.json

      - name: Install dependencies
        shell: pwsh
        run: |
          npm ci --prefix commun/backend
          npm ci --prefix web/app
          npm ci --prefix ios/apptel

      - name: Prepare Vite preproduction env
        shell: pwsh
        run: |
          Copy-Item web/app/.env.preproduction.example web/app/.env.preproduction -Force
          Write-Host "web/app/.env.preproduction ready (from example)"

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.STAGING_SSH_PRIVATE_KEY }}

      - name: Prepare staging SSH key file (OpenSSH Windows)
        shell: pwsh
        env:
          STAGING_SSH_PRIVATE_KEY: ${{ secrets.STAGING_SSH_PRIVATE_KEY }}
        run: |
          if (-not $env:STAGING_SSH_PRIVATE_KEY) {
            throw 'STAGING_SSH_PRIVATE_KEY secret manquant'
          }
          $keyPath = Join-Path $env:RUNNER_TEMP 'staging_deploy_ed25519'
          $keyText = $env:STAGING_SSH_PRIVATE_KEY.TrimEnd() + "`n"
          [System.IO.File]::WriteAllText($keyPath, $keyText, [System.Text.UTF8Encoding]::new($false))
          icacls $keyPath /inheritance:r /grant:r "$($env:USERNAME):(R)" | Out-Null
          "DEPLOY_SSH_KEY=$keyPath" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          Write-Host "Cle staging materialisee : $keyPath"

      - name: Deploy preprod (zero-downtime)
        shell: pwsh
        env:
          DEPLOY_SSH_HOST: root@51.159.170.181
        run: ./commun/scripts/deploy-preprod.ps1

      - name: Verify staging health
        shell: pwsh
        run: |
          $urls = @(
            'http://51.159.170.181/health',
            'https://staging.onscen.com/health'
          )
          $ok = $false
          foreach ($u in $urls) {
            try {
              $r = Invoke-WebRequest -Uri $u -UseBasicParsing -TimeoutSec 30
              if ($r.StatusCode -eq 200 -and $r.Content -match 'preproduction') {
                Write-Host "OK $u"
                Write-Host $r.Content
                $ok = $true
                break
              }
            } catch {
              Write-Host "Skip $u : $($_.Exception.Message)"
            }
          }
          if (-not $ok) { throw 'Health staging echoue (preproduction attendu)' }
