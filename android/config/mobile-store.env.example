# Copier vers mobile-store.env (gitignoré) et renseigner APPLE_TEAM_ID
# Générer SHA256 release : node scripts/update-well-known-mobile.mjs --from-keystore

ANDROID_RELEASE_SHA256=REPLACE_WITH_RELEASE_SHA256
APPLE_TEAM_ID=REPLACE_WITH_APPLE_TEAM_ID
