# Audit Stripe — Soundy

## 1. Résumé exécutif

L'intégration Stripe de Soundy est **fonctionnellement correcte et globalement bien architecturée** : webhooks signés avec `express.raw()` isolé avant le parseur JSON global, Stripe Connect en *destination charges* avec `application_fee_amount`, formulaire de paiement via `PaymentElement` (aucune donnée carte ne transite par le frontend/backend Soundy), secrets non commités, séparation claire simulation (msdev) / réel (prod).

Le contexte fourni est **confirmé par le code** : le split 70/30 (host/Soundy) sur les pourboires existe bien via `application_fee_amount` + `transfer_data.destination` dans `commun/backend/src/routes/donations.ts:369-376`, avec un défaut de **30 %** de commission plateforme (`commun/backend/src/config/donationLegal.ts:2`). La vérification `charges_enabled` avant création du PaymentIntent existe aussi, à `commun/backend/src/routes/donations.ts:351-366`. En revanche, les **abonnements créateurs utilisent un taux différent** (10 % par défaut via `application_fee_percent`, `commun/backend/src/lib/subscriptions.ts:57-63`) — ce n'est pas un 70/30 partout, seulement sur les dons.

Les lacunes les plus sérieuses ne sont pas des failles de sécurité classiques (pas de clé committée, pas de manipulation de carte côté app) mais des **problèmes de fiabilité financière et opérationnelle** : absence totale de clés d'idempotence Stripe, absence totale de remboursements programmatiques, une incohérence de nommage de variable d'environnement qui peut fausser silencieusement les montants affichés/enregistrés pour l'abonnement "Soundy+", et un risque réel de double-crédit d'un don/abonnement en environnement PM2 cluster (2 workers) du fait d'une déduplication de webhook uniquement en mémoire locale par process.

## 2. Tableau des problèmes

| # | Gravité | Fichier(s) / Ligne(s) | Description | Solution | Difficulté |
|---|---|---|---|---|---|
| 1 | **High** | `commun/backend/src/routes/donations.ts:369` (`paymentIntents.create`), `commun/backend/src/routes/subscriptions.ts:330` (`checkout.sessions.create`) | Aucune clé d'idempotence (`idempotencyKey`) transmise sur les appels Stripe créant une ressource facturable. Grep `idempotenc` sur tout `commun/backend/src` = **0 résultat**. En cas de retry réseau (timeout client, double clic, reconnexion mobile), un second PaymentIntent/Checkout Session peut être créé pour la même intention utilisateur. | Générer une clé d'idempotence déterministe côté serveur (ex. hash de `userId+liveId+amount+minute`) et la passer en 2ᵉ argument des appels `stripe.paymentIntents.create()` / `stripe.checkout.sessions.create()`. | S |
| 2 | **High** | `commun/deploy/ecosystem.config.cjs:32-33` (2 instances, `cluster`), `commun/backend/src/bootstrap.ts:339,351` (chargement PG **une seule fois** au boot), `commun/backend/src/routes/donations.ts:469` (dédup `db.gifts.find(...)` en mémoire) | Le state `db.gifts` / `db.creatorSubscriptions` est un tableau **en mémoire par process**, chargé une fois au démarrage et jamais resynchronisé. En prod, 2 workers PM2 cluster écoutent le même port 3000 (`commun/deploy/Caddyfile:19` proxy unique) — Node répartit les requêtes entre workers. Si Stripe relivre un webhook (retry standard sur non-200 ou timeout) et qu'il atterrit sur l'autre worker, la vérification anti-doublon locale ne voit pas l'enregistrement déjà créé par le premier worker → **double crédit possible** d'un don (double notification hôte, double compteur admin) ou double traitement d'un événement d'abonnement. | Faire reposer la déduplication sur une contrainte unique en base (`UNIQUE(payment_intent_id)` / `UNIQUE(stripe_subscription_id)`) avec `ON CONFLICT DO NOTHING`, ou vérifier en base (pas seulement en mémoire) avant d'écrire. | M |
| 3 | **High** | Aucun résultat pour `refund`/`Refund` dans `commun/backend/src` (hors clé i18n `donationLegal.ts:16`) ; aucune route admin de remboursement | Aucun appel `stripe.refunds.create` n'existe dans le code, et aucune route/bouton admin ne permet de rembourser un don ou un abonnement. Or les mentions légales affichées à l'utilisateur (`web/app/src/locales/fr.json:2360`) promettent : « Les pourboires validés ne sont en principe pas remboursables, sauf erreur manifeste, fraude ou obligation légale » — cette exception n'est outillée nulle part dans le produit. | Ajouter une route admin authentifiée `POST /api/admin/donations/:id/refund` appelant `stripe.refunds.create({ payment_intent })`, avec journalisation et autorisation stricte (rôle admin). | M |
| 4 | **High** | Code : `commun/backend/src/lib/subscriptions.ts:72,77,99` (`SUBSCRIPTION_SOUNDY_PLUS_AMOUNT_EUR`, `SUBSCRIPTION_SOUNDY_PLUS_LABEL`, `STRIPE_PRICE_ID_SOUNDY_PLUS` — sans « L ») vs docs/scripts : `commun/msdev/.env.example:142,145`, `commun/scripts/secrets-checklist.template.txt:23`, `commun/scripts/setup-stripe-msdev.ps1:93-94`, `commun/docs/reports/acompleter.txt:157-159` (`SOUNDLY` — avec « L ») | Incohérence de nommage entre le code et la documentation/scripts officiels utilisés pour configurer l'environnement. Un opérateur qui suit `.env.example` ou le checklist de secrets, ou qui exécute `setup-stripe-msdev.ps1` (qui écrit lui-même `SUBSCRIPTION_SOUNDLY_PLUS_AMOUNT_EUR` à la ligne 93), configurera une variable jamais lue par le code, qui retombera silencieusement sur les valeurs par défaut codées en dur (999 / 1999 centimes, `subscriptions.ts:70-73`). Le prix réellement facturé par Stripe (déterminé par le `Price` object) ne sera pas affecté, mais les montants affichés/enregistrés en base (`amountCents`, revenus admin) peuvent diverger silencieusement du prix réel. | Harmoniser un seul nom (`SOUNDY` recommandé, cohérent avec le code) dans tous les fichiers `.env.example`, scripts et docs ; ajouter un test qui échoue si une variable `*_SOUNDLY_*` est détectée dans `process.env` sans équivalent `*_SOUNDY_*`. | S |
| 5 | **Medium** | `commun/backend/src/lib/productionStartup.ts` (fonction complète, l. 28-118) — aucune vérification de `STRIPE_WEBHOOK_SECRET` / `STRIPE_SUBSCRIPTION_WEBHOOK_SECRET` ; `commun/backend/src/lib/productionStartup.ts:101-106` — seul un `console.warn` (pas de blocage) si `STRIPE_SECRET_KEY` commence par `sk_test_` en prod | `assertProductionStartup()` fait échouer le boot pour JWT/ENCRYPTION_KEY/DATABASE_URL/SENTRY_DSN manquants, mais ne bloque jamais un démarrage prod avec Stripe mal configuré (webhook secret absent) ou en mode test. Une alerte admin existe bien (`commun/backend/src/lib/prodSaasStatus.ts:112-118`, sévérité "critical") mais elle est seulement visible dans le panel admin, pas bloquante. | Ajouter dans `assertProductionStartup()` un `throw` si `DONATIONS_ENABLED=1` ou `SUBSCRIPTIONS_ENABLED=1` sans `STRIPE_WEBHOOK_SECRET` correspondant configuré. | S |
| 6 | **Medium** | Grep `payment_failed`/`invoice.payment` sur `commun/backend/src` = 0 résultat ; `commun/backend/src/routes/subscriptions.ts:517-529` ne traite que `active`/`trialing`/`canceled`/`unpaid` | Aucun traitement de l'événement `invoice.payment_failed`. L'utilisateur n'est pas notifié proactivement d'un échec de paiement (pas de dunning applicatif). L'impact réel est atténué car `currentPeriodEnd` n'étant pas prolongé sans `invoice.paid`, l'accès expire naturellement à la fin de période — mais sans avertissement ni relance en amont. | Écouter `invoice.payment_failed`, notifier l'utilisateur (email/notif in-app) et exposer un lien vers le Customer Portal pour mettre à jour la carte. | M |
| 7 | **Medium/Low** | 7 instanciations `new Stripe(key)` sans `apiVersion` : `commun/backend/src/routes/donations.ts:36`, `commun/backend/src/routes/subscriptions.ts:40`, `commun/backend/src/lib/donations.ts:94`, `commun/backend/src/lib/accountDeletionStripe.ts:8`, `commun/backend/src/lib/healthChecks.ts:39` | Aucun `apiVersion` fixé lors de l'instanciation du SDK Stripe. Le comportement de l'API peut varier silencieusement si la version par défaut du compte Stripe change (upgrade dashboard), sans que le code ne soit mis à jour en conséquence. | Créer une factory unique `getStripeClient()` centralisée avec `{ apiVersion: '2024-XX-XX' }` pinné, et remplacer les 7 instanciations dupliquées. | S |
| 8 | **Low** | Aucun test HTTP sur `handleStripeDonationWebhook` / `handleStripeSubscriptionWebhook` (seuls des tests unitaires de logique métier existent : `donations.test.ts`, `subscriptions.test.ts`) | Pas de couverture automatisée pour : signature invalide → 400, event.type inconnu → 200 no-op, double livraison du même `payment_intent.succeeded`. | Ajouter des tests d'intégration simulant `stripe.webhooks.constructEvent` avec un mock de signature. | M |
| 9 | **Low** | `commun/backend/src/routes/donations.ts:169` (`country: 'FR'` en dur dans `stripe.accounts.create`) | Les comptes Connect Express sont systématiquement créés en France ; pas de support de créateurs basés hors France. Limitation produit, pas une vulnérabilité. | Paramétrer le pays selon le profil utilisateur si l'expansion internationale est prévue. | S |
| 10 | **Low** | `commun/scripts/stripe-connect-setup.sh:9,11,62` (identifiant de compte Connect de production `acct_1ThwQ2FsKQ6HX3Pk`, `user_id` réel, email admin, en clair dans un script versionné) | Fuite d'identifiants internes de production (non-secrets Stripe au sens strict, mais informations opérationnelles identifiantes) dans l'historique Git. | Paramétrer ces valeurs via variables d'environnement/arguments uniquement, sans valeur par défaut codée en dur. | S |
| 11 | **Info (positif)** | `commun/backend/src/server.ts:349-358` | Webhooks montés avec `express.raw({ type: 'application/json' })` avant `express.json()` global (`server.ts:367`) → corps brut préservé pour la vérification de signature. Bonne pratique respectée. | — | — |

## 3. Score du domaine : 61 / 100

| Sous-domaine | Note | Justification |
|---|---|---|
| Webhooks (signature, raw body) | 80/100 | `constructEvent` correctement utilisé avec secret dédié par route, raw body isolé. Pénalité : pas de gestion `event.livemode`, pas de test automatisé. |
| Idempotence | 20/100 | Aucune `idempotencyKey` Stripe nulle part ; dédup applicative fragile en environnement multi-process. |
| Checkout vs PaymentIntent | 75/100 | Usage cohérent : PaymentIntent + Elements pour les dons, Checkout Session pour les abonnements. |
| Cycle de vie abonnement | 55/100 | Création/renouvellement/annulation gérés ; `invoice.payment_failed` absent ; pas de dunning proactif. |
| Customer Portal | 85/100 | Implémenté et sécurisé par JWT + vérification d'appartenance. |
| Metadata | 85/100 | Cohérente et utilisée pour relier PaymentIntent/Session ↔ DB. |
| Secrets | 90/100 | Aucun secret réel commité, `.env.example` propres, `.gitignore` correct. |
| Test/Live mode | 70/100 | Détection existe + alerte admin critique, mais pas de blocage au boot. |
| Produits/Prix | 50/100 | Prix Stripe dynamiques, mais bug de nommage env impactant l'affichage/l'enregistrement. |
| Gestion des erreurs | 75/100 | `try/catch` systématique, messages traduits ; pas de retry logic applicatif. |
| Remboursements | 5/100 | Non implémentés du tout alors que promis conditionnellement dans les CGU. |
| Stripe Connect | 80/100 | Onboarding Express + vérification `charges_enabled`/`details_submitted` avant paiement. |
| PCI / frontend | 100/100 | `PaymentElement` exclusivement ; aucune manipulation de numéro de carte côté Soundy. |

## 4. Points impossibles à vérifier avec les informations disponibles

- La valeur réelle de `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` sur le VPS de production.
- Le mode effectif (live vs test) du compte Stripe actuellement utilisé en production.
- La configuration réelle du endpoint webhook côté Dashboard Stripe.
- La fréquence réelle d'occurrence de la course entre workers PM2 en production (risque démontré architecturalement, pas mesuré en conditions réelles).
- Si des remboursements sont actuellement traités manuellement par un administrateur via le Dashboard Stripe (processus hors-code).
- La conformité juridique précise de la clause de remboursement conditionnelle au droit français/européen.

## Synthèse rapide

- **Problèmes par gravité** : Critical : 0 · High : 4 · Medium : 3 · Low : 4 (+1 point positif)
- **Score du domaine : 61/100**
